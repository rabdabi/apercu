# Kurzfilm 01 — „Emil geht"

**Phase-2 first short** (see `docs/STATUS.md` movie plan): a ~15–20 s *moving
illustration*, not a full episode. Purpose is twofold:

1. **Learning piece** — your first Moho project: one walk cycle, one background plate,
   one slow pan, deliberate 2-frame holds. Small enough to finish.
2. **Usable asset** — exports as a seamless loop for an `AmbientClip` on the site (drop
   into a slot per `docs/media-manifest.md`).

No `episode.mdx` here on purpose — this is a production artifact, not a published page.

Pipeline files (gates 01–03, produced by Claude): `brief.md` · `script.md` ·
`canon-report.md` · `shotlist.md` · `moho-scenes.json`. Gate 05 (Moho) is yours.
Export target: `public/media/emil-geht.webm` (+ poster).
