# <CHARACTER> — pose library. Align names to the character's model sheet in
# content-bible/characters/<slug>.md and to any Rive states. moho_action stays
# null until the master rig exists (filled by rediscovery, never from memory).
character_id: CHAR-XXXX-001
status: TODO-no-rig-yet
poses:
  neutral_front: { moho_action: null }
  neutral_3q:    { moho_action: null }
  neutral_side:  { moho_action: null }
  # add poses this character actually needs
