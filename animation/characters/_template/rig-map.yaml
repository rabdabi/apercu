# <CHARACTER> — Moho rig map (TECHNICAL layer only). Copy to characters/<id>/.
# Canon lives in content-bible/characters/<id>.md — that file wins on any conflict.
# Never invent bone/layer IDs: they are rediscovered at runtime (session-scoped).

character_id: CHAR-XXXX-001
canon: content-bible/characters/<slug>.md
moho_master: animation/moho/masters/<slug>-master.moho
version: 0.0.0
status: TODO-no-rig-yet

bone_layer: null
bones: {}            # expected bone NAMES; IDs resolved live
switch_layers: {}    # mouth / hands / eyes switch layers
smart_bones: {}      # Smart Bone dials
actions: {}          # see actions.yaml
